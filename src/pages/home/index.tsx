import Styles from './Home.module.scss';

export default function Home () {
    return (
        <section className={Styles.top}>
            <h1 className={Styles.top__title}>costa fria</h1>
        </section>
    );
}